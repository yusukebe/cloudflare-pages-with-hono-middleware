{
  "name": "cloudflare-pages-with-hono-middleware",
  "scripts": {
    "dev": "wrangler pages dev ./pages",
    "deploy": "wrangler pages deploy ./pages"
  },
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240722.0",
    "wrangler": "^3.65.1"
  },
  "dependencies": {
    "@elithrar/workers-hono-rate-limit": "^0.4.3",
    "hono": "^4.5.1"
  }
}